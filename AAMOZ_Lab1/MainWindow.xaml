<Window x:Class="ImageLocalization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="Локалізація об'єкта" Height="950" Width="1300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Панель кнопок -->
            <RowDefinition Height="*"/>
            <!-- Основна робоча область -->
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Завантажити" Click="BtnLoad_Click" Padding="15,5" Margin="0,0,10,0"/>
            <Button Content="Аналіз" Click="BtnProcess_Click" Padding="15,5" Background="#E3F2FD"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <!-- Зона аналізу (Оригінал + Проєкції) -->
                <ColumnDefinition Width="*"/>
                <!-- Зона порівняння (ЧБ + Сірий) -->
            </Grid.ColumnDefinitions>

            <!-- 1. Основна зона аналізу (Згідно з вашим прикладом) -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="Black" BorderThickness="1">
                    <Image x:Name="ImgDisplay" Stretch="Fill" Margin="35,17,15,17"/>
                </Border>

                <ScottPlot:WpfPlot x:Name="PlotVertical" Grid.Column="1"/>
                <ScottPlot:WpfPlot x:Name="PlotHorizontal" Grid.RowSpan="2" Height="200" VerticalAlignment="Bottom"/>
            </Grid>

            <!-- 2. Зона порівняння (Пункт 4 завдання) -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox Header="Чорно-біле (Пункт 2)" Height="300" Margin="0,0,0,10">
                    <Image x:Name="ImgBinary" Stretch="Uniform"/>
                </GroupBox>
                <GroupBox Header="Відтінки сірого (Пункт 3)" Height="300">
                    <Image x:Name="ImgGray" Stretch="Uniform"/>
                </GroupBox>

                <!-- Статистика -->
                <GroupBox Header="Статистичні дані" Margin="0,10,0,0">
                    <TextBlock x:Name="TxtAllStats" TextWrapping="Wrap" Padding="5"/>
                </GroupBox>
            </StackPanel>
        </Grid>
    </Grid>
</Window>